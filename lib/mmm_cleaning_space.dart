enum RecordType {
  follow,
  unfollow,
  call,
  share,
  view,
  save,
  unSave,
  review,
  editReview,
  deleteReview,
  question,
  editQuestion,
  deleteQuestion,
  answer,
  editAnswer,
  deleteAnswer,
  search,
}

enum ModelType{
  flyer,
  bz,
  question,
  answer,
  search,
}

enum RecordDetailsType{
  slideIndex,
  searchText,
}
