import 'package:flutter/material.dart';

class EditorSlideImage extends StatefulWidget {

  const EditorSlideImage({
    @required this.flyerBoxWidth,
    Key key
  }) : super(key: key);

  final double flyerBoxWidth;

  @override
  _EditorSlideImageState createState() => _EditorSlideImageState();
}

class _EditorSlideImageState extends State<EditorSlideImage> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
